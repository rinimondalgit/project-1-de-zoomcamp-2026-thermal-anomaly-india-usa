name: 'thermal_anomaly_project'
version: '1.0.0'
config-version: 2

profile: 'thermal_anomaly_project'

model-paths: ["models"]
seed-paths: ["seeds"]

models:
  thermal_anomaly_project:
    staging:
      +materialized: view
    marts:
      +materialized: table
